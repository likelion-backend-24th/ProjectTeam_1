# 2026-08-19 결제(f-110-payment) 브랜치 점검 노트

> 브랜치: `feat/f-110-payment` (backend / frontend-kjb 공통)
> 목적: 현재 브랜치 상태에서 백엔드·프론트가 각각 더 구현해야 할 것, 잘못 작동할 것으로 의심되는 부분, 팀이 결정해야 할 의문점을 정리. ChatGPT와 같이 코드 짤 때 컨텍스트로 사용.
> 이 문서는 코드 변경 없이 조사만 한 결과입니다.

---

## 0. 현재 로컬 diff (커밋 안 된 변경분)

- `backend/src/main/java/com/team1/cityfarm/controller/OrderController.java`
- `backend/src/main/java/com/team1/cityfarm/repository/OrderRepository.java`
- `backend/src/main/java/com/team1/cityfarm/service/OrderService.java`

세 파일은 "주문 생성/단건 조회/내 주문 목록 조회"를 되살려서 구현한 변경. 이 변경 자체는 얼추 동작하지만 아래 2장에서 짚은 회귀 버그가 하나 섞여 있음.

이 문서는 위 diff만이 아니라, 같은 결제 도메인(Order/Payment/Subscription/BillingKey/Webhook) 전체를 훑어서 정리한 것.

---

## 1. 백엔드 — 구현해야 할 것

1. **`SubscriptionController`가 아예 없음.**
   `SubscriptionService`(`getMyActiveSubscription`, `createSubscription`, `cancelAtPeriodEnd`, `getPass`)는 이미 완성돼 있는데 이걸 노출하는 컨트롤러가 없다. 프론트(`frontend-kjb/src/lib/api/subscription.js`)는 이미 아래 4개 경로를 호출하도록 짜여 있으므로, 백엔드는 이 경로/시그니처에 맞춰 컨트롤러만 얹으면 됨.
   - `GET /api/subscriptions/me` → `SubscriptionService.getMyActiveSubscription`
   - `POST /api/subscriptions` (body: `{ planType, billingKeyId }`) → `SubscriptionService.createSubscription`
   - `POST /api/subscriptions/{id}/cancel` → `SubscriptionService.cancelAtPeriodEnd`
   - `GET /api/subscriptions/{id}/passes` → `SubscriptionService.getPass` (프론트는 배열 응답을 기대하고 `passes?.[0]`을 씀 — 응답 형태를 배열로 할지 단건으로 할지 프론트와 맞춰야 함, 3장 질문 참고)

2. **구독 최초 결제(빌링키 첫 결제) 흐름이 비어 있음.**
   `SubscriptionService.createSubscription`은 빌링키 존재 여부만 확인하고 곧장 `Subscription`을 `ACTIVE`로 만들어버림. `PortonePaymentClient.executeBillingPayment(...)`를 호출해 실제 카드 결제를 시도하는 코드가 없고, `Order`/`Payment` 레코드도 생성하지 않음.
   반면 `PortoneWebhookService.handlePaymentPaid`는 `Payment → Order → Subscription`을 거쳐 구독을 연장하는 구조라, 최초 구독 시점에 대응하는 `Payment`/`Order`가 없으면 웹훅이 와도 매칭이 안 됨(`findByPortonePaymentId` 실패로 조용히 무시됨, `PortoneWebhookService.java:62-68`).
   → 구독 최초 결제도 "Order 생성 → PortOne 결제/검증 → Payment 저장" 경로를 태우게 만들지, 아니면 빌링키 결제는 별도 플로우로 갈지 설계 필요 (3장 질문 참고).

3. **정기(매월) 자동결제를 실행하는 배치/스케줄러가 없음.**
   `SubscriptionSchedule` 엔티티/레포지토리, `PaymentTransactionRepository`는 존재하지만 어떤 서비스에서도 참조되지 않음(grep 결과 0건). `PortonePaymentClient.executeBillingPayment`도 어디서도 호출되지 않음. 즉 "다음 달 자동결제"를 트리거하는 코드가 현재 없음 — 스케줄러(예: `@Scheduled`) 또는 PortOne 예약결제 API 사용 여부를 결정하고 구현 필요.

4. **Webhook 서명 검증 및 멱등성 처리가 비어 있음 (보안 이슈).**
   `PortoneWebhookController`가 `x-portone-signature` 헤더를 받기만 하고 검증 로직이 없음. `WebhookInbox` 엔티티(webhookId 유니크 제약, status, payload 저장용)가 있는데 `PortoneWebhookService`는 이걸 전혀 쓰지 않음 → 아무나 `/api/webhooks/portone`에 위조된 `Transaction.Paid` 이벤트를 POST하면 결제 완료 처리가 그대로 통과됨. 서명 검증 + `WebhookInbox`로 중복/재시도 처리하는 로직 추가 필요.

5. **`OrderService.createClassOrder`가 중복 신청 검증을 빼먹은 채로 재구현됨 (회귀).**
   기존에는 `ClassEnrollmentService.createPendingEnrollment`를 호출했고, 그 안에 `existsByOneDayClassIdAndUserIdAndStatusIn(...)` 로 이미 `PENDING`/`CONFIRMED` 신청이 있는지 검증하는 로직이 있었음(`ClassEnrollmentService.java:38-46`). 그런데 이번 diff에서 `OrderService.createClassOrder`가 이 서비스 호출 대신 `ClassEnrollment.builder()...`로 직접 엔티티를 만들면서 중복 검증 코드가 빠짐. 지금 상태로는 같은 유저가 같은 클래스에 여러 번 주문을 생성할 수 있음.
   → `OrderService`가 다시 `classEnrollmentService.createPendingEnrollment(...)`를 호출하도록 되돌리거나, 동일한 중복 체크를 `OrderService`에도 추가해야 함.

6. **에러용 HTTP 상태 코드가 잘못 매핑된 게 있음.**
   `CustomError.PAYMENT_NOT_FOUND`가 `HttpStatus.NO_CONTENT`(204), `CustomError.INVALID_PAYMENT_AMOUNT`가 `HttpStatus.NOT_MODIFIED`(304)로 정의돼 있음(`CustomError.java:52,58`). 둘 다 "본문 없음/캐시" 의미의 상태코드라 에러 메시지를 담아 보내는 용도로 쓰면 안 됨. 프론트가 실제로 이 상태코드를 어떻게 오작동시키는지는 2장 참고. `404`/`400` 등으로 바꾸는 게 맞아 보임.

7. **테스트 코드 전무.**
   `OrderService`/`PaymentService`/`PortoneWebhookService`/`SubscriptionService` 어느 것도 단위 테스트가 없음(`backend/**/test/**` 검색 결과 0건). 결제 검증 로직(금액 위변조 체크, 상태 전이)처럼 리스크가 큰 로직부터 최소한의 테스트를 붙이는 게 좋음.

8. **사소한 정리 대상.**
   - `OrderService.java`에 `import com.team1.cityfarm.repository.*;`가 8번째 줄과 15번째 줄 두 번 중복(컴파일은 되지만 정리 필요).
   - `OrderController.createOrder`가 기존 `HttpStatus.CREATED`(201) 응답에서 `ResponseEntity.ok`(200)로 바뀜 — 의도적인지 확인 필요(3장 질문).
   - `Order`/`Payment`/`BillingKey` 계열 컨트롤러는 성공 응답을 `ApiResponse`로 감싸지 않고 DTO를 그대로 반환(`OrderController`, `PaymentController`, `BillingKeyController`). 반면 `Board`/`Comment`/`Profile` 계열은 `ApiResponse.success(...)`로 감쌈. 지금은 프론트 `apiRequest`가 우연히 둘 다 잘 처리하고 있지만(3장 참고), 팀 컨벤션을 통일할지 여부는 결정 필요.

---

## 2. 프론트엔드 — 구현해야 할 것 / 잘못 작동할 것으로 의심되는 부분

1. **결제창 자체가 아직 동작 안 함 (의도된 미구현).**
   `frontend-kjb/src/lib/portone/client.js`의 `requestPayment()`, `requestIssueBillingKey()`는 PortOne SDK 로드까지만 하고 `NOT_IMPLEMENTED` 에러를 던지도록 스텁으로 남아 있음. `NEXT_PUBLIC_PORTONE_STORE_ID`/`NEXT_PUBLIC_PORTONE_CHANNEL_KEY`와 실제 `PortOne.requestPayment(...)` 호출부를 채워야 결제 버튼이 실제로 동작함.

2. **주문 생성(`createOrder`)을 호출하는 화면이 프론트 어디에도 없음.**
   `frontend-kjb/src/lib/api/order.js`의 `createOrder(payload)`는 정의만 돼 있고, 코드 전체에서 이 함수를 호출하는 곳이 없음(grep 결과 정의부 1건뿐). 원데이클래스 목록/상세 페이지 자체가 `frontend-kjb/src/app` 아래에 없어서, "클래스를 골라서 신청 → `/payment/checkout?orderId=...`로 이동"하는 진입점이 통째로 빠져 있음. `f-150-class` 쪽 클래스 상세 화면이 만들어지면 거기서 `createOrder({ classId, orderType: "GENERAL" })` 호출 후 `router.push('/payment/checkout?orderId=' + order.id)` 형태로 연결해야 함.

3. **결제 검증 요청의 필드명이 백엔드 DTO와 다름 — 실사용 시 100% 실패하는 버그.**
   - 프론트: `frontend-kjb/src/app/payment/checkout/page.js:64` → `verifyPayment({ orderId: order.id, paymentId: result.paymentId })`
   - 백엔드: `PaymentVerifyRequestDto`는 `{ paymentId, merchantOrderId }` 필드만 읽음(`PaymentVerifyRequestDto.java`), `orderId`라는 필드가 없음.
   - 결과: `merchantOrderId`가 항상 `null`로 전달되고, `PaymentService.verifyAndCompletePayment`가 `orderRepository.findByMerchantOrderId(null)`을 호출해 무조건 `ORDER_NOT_FOUND` 예외가 남(`PaymentService.java:48-49`).
   - PortOne 연동이 아직 안 돼 있어(위 1번) 지금은 드러나지 않지만, `requestPayment`를 실제로 붙이는 순간 결제 검증이 항상 실패하게 됨. `order.merchantOrderId`(문자열)를 보내도록 프론트를 고치거나, 백엔드가 숫자 `orderId`도 받아주도록 DTO를 넓히는 방향 중 하나로 맞춰야 함.

4. **`getOrder`, `getMyOrders`, `refund-eligibility` 경로는 "추정 경로"라는 주석이 붙어 있는데 이미 백엔드에 실존함.**
   `frontend-kjb/src/lib/api/order.js`, `payment.js`에 "OrderController가 아직 없어 404 날 수 있다", "경로가 확정되지 않았다" 같은 TODO 주석이 남아 있는데, 지금 diff로 `OrderController`(`GET /api/orders/{orderId}`, `GET /api/orders/me`)와 `PaymentController`(`GET /api/payments/{paymentId}/refund-eligibility`)가 모두 구현돼 있어 경로가 실제로 맞음. 주석만 stale한 상태라 정리하면 됨. (단, 3번 항목의 필드명 문제는 별개로 남아 있음)

5. **구독 관련 4개 API는 전부 백엔드 미구현 상태 — 프론트도 이미 인지하고 있음.**
   `frontend-kjb/src/lib/api/subscription.js` 주석에 "SubscriptionController가 아직 없어 404가 발생할 수 있다"고 명시돼 있고, 실제로 1장 1번에서 확인한 대로 컨트롤러가 없음. `/subscription`, `/subscription/billing` 페이지는 만들어져 있지만 백엔드 붙기 전까지는 전부 실패함.

6. **`getSubscriptionPasses` 응답 형태 가정이 백엔드 시그니처와 안 맞을 수 있음.**
   프론트 `subscription/page.js:34-35`는 `getSubscriptionPasses(sub.id)`가 배열을 반환한다고 가정하고 `passes?.[0]`을 꺼내 씀. 반면 백엔드 `SubscriptionService.getPass(subscriptionId)`는 단건(`SubscriptionPassDto`) 하나만 반환함. 컨트롤러를 만들 때 배열로 감쌀지, 프론트를 단건 응답에 맞게 고칠지 정해야 함(3장 질문).

7. **204 / 304 에러 상태코드를 프론트가 "성공"으로 오인할 수 있음.**
   `frontend-kjb/src/lib/api/client.js:60-62` — `apiRequest`는 `res.status === 204`면 무조건 `undefined`를 반환(에러 바디를 읽지 않음). `fetch`의 `res.ok`는 200~299를 모두 성공으로 취급하므로, 백엔드가 `PAYMENT_NOT_FOUND`를 204로 내려보내면 프론트는 이걸 에러가 아니라 "성공, 데이터 없음"으로 처리해버림. 결제 취소(`CancelPaymentButton.js`)나 환불 가능 여부 조회 화면에서 "결제 정보 없음" 에러가 조용히 씹히고 엉뚱한 화면이 뜰 수 있음. 근본 원인은 백엔드 상태코드 매핑 문제(1장 6번)라, 백엔드 수정이 우선.

8. **`getSubscriptionPassUsages`는 프론트도 명시적으로 `NOT_IMPLEMENTED`.**
   백엔드에 해당 조회 API가 없다는 걸 인지하고 있고, 함수 자체가 항상 reject되도록 스텁으로 남아 있음(`subscription.js:30-34`). 백엔드가 API를 만들면 본문만 `apiRequest` 호출로 교체하면 되는 구조.

---

## 3. 팀/ChatGPT와 같이 정할 의문점

1. **결제 검증 요청 계약을 뭘로 통일할까?**
   프론트는 `merchantOrderId`(주문번호 문자열) 대신 숫자 `order.id`를 이미 들고 있는 상태에서 호출함. `PaymentVerifyRequestDto`를 `orderId`(Long)도 받게 넓힐지, 프론트가 `order.merchantOrderId`를 넘기도록 고칠지 — 어느 쪽이 PortOne 콜백 파라미터(`paymentId`가 결제창에서 그대로 나오는 값)와 더 자연스럽게 맞물리는지 확인 필요.

2. **구독 최초 결제(빌링키 첫 과금)도 `Order`/`Payment`를 만들 것인가?**
   지금 `SubscriptionService.createSubscription`은 결제 없이 바로 `ACTIVE`로 만듦. PortOne 빌링키 결제(`executeBillingPayment`)를 동기로 호출하고 성공 시 `Order(orderType=SUBSCRIPTION)` + `Payment`를 만들지, 아니면 웹훅 비동기 처리로만 갈지 방향을 정해야 웹훅 쪽 `order.getSubscription()` 매칭 로직도 앞뒤가 맞음.

3. **정기 자동결제(매월 갱신)는 스케줄러로 직접 돌릴지, PortOne의 "예약결제/구독" 기능에 맡길지?**
   `SubscriptionSchedule` 엔티티가 이미 있는 걸 보면 자체 스케줄러를 염두에 두고 설계된 것 같은데, 실제 실행 주체(Spring `@Scheduled`, 별도 배치, 아니면 PortOne 측 기능)가 뭔지 결정이 안 나 있음.

4. **Webhook 서명 검증 방식과 `WebhookInbox` 사용 여부.**
   PortOne V2 웹훅 서명 검증(표준: `webhook-id`/`webhook-timestamp`/`webhook-signature` 헤더 기반 HMAC 검증)을 어떤 라이브러리/방식으로 구현할지, `WebhookInbox`에 먼저 적재 후 비동기 처리할지 동기 처리하되 멱등키만 체크할지 정해야 함.

5. **에러 응답 포맷을 전 컨트롤러에서 `ApiResponse`로 통일할 것인가?**
   지금 `Order`/`Payment`/`BillingKey`는 raw DTO, `Board`/`Comment`/`Profile`은 `ApiResponse.success(...)`. 프론트 `apiRequest`가 "`data` 필드 있으면 언랩, 없으면 그대로 반환" 방식이라 우연히 둘 다 동작은 하는데, 앞으로 계속 이 두 스타일이 섞여도 되는지 컨벤션을 정해야 함.

6. **`GET /api/subscriptions/{id}/passes` 응답을 배열로 줄지 단건으로 줄지.**
   서비스 메서드명(`getPass`, 단수)과 프론트의 배열 가정이 어긋나 있음. "한 구독에 활성 수강권(Pass)이 항상 1개"라는 도메인 규칙이 맞다면 단건 응답 + 프론트 수정이 더 자연스러워 보임 — 확인 필요.

7. **`OrderController.createOrder` 응답 코드를 201로 되돌릴지.**
   REST 컨벤션상 생성 API는 201이 일반적인데 이번 diff에서 200으로 바뀜. 의도적 변경인지, 단순 실수인지 확인 필요.

8. **클래스 신청 진입 화면(원데이클래스 목록/상세)은 어느 브랜치/누가 담당하나.**
   결제 플로우 전체가 이 화면 없이는 테스트조차 불가능함. `f-150-class` 브랜치와의 병합 순서/일정 확인 필요.

---

## 4. 참고 파일 목록

**Backend**
- `backend/src/main/java/com/team1/cityfarm/controller/OrderController.java`
- `backend/src/main/java/com/team1/cityfarm/controller/PaymentController.java`
- `backend/src/main/java/com/team1/cityfarm/controller/PortoneWebhookController.java`
- `backend/src/main/java/com/team1/cityfarm/controller/BillingKeyController.java`
- `backend/src/main/java/com/team1/cityfarm/service/OrderService.java`
- `backend/src/main/java/com/team1/cityfarm/service/PaymentService.java`
- `backend/src/main/java/com/team1/cityfarm/service/PortoneWebhookService.java`
- `backend/src/main/java/com/team1/cityfarm/service/SubscriptionService.java` (컨트롤러 없음)
- `backend/src/main/java/com/team1/cityfarm/service/ClassEnrollmentService.java`
- `backend/src/main/java/com/team1/cityfarm/service/SettlementService.java`
- `backend/src/main/java/com/team1/cityfarm/portone/PortonePaymentClient.java`
- `backend/src/main/java/com/team1/cityfarm/global/exception/CustomError.java`
- `backend/src/main/java/com/team1/cityfarm/global/response/ApiResponse.java`
- `backend/src/main/java/com/team1/cityfarm/entity/WebhookInbox.java` (미사용)
- `backend/src/main/java/com/team1/cityfarm/entity/SubscriptionSchedule.java` (미사용)

**Frontend (frontend-kjb)**
- `frontend-kjb/src/lib/api/order.js`
- `frontend-kjb/src/lib/api/payment.js`
- `frontend-kjb/src/lib/api/subscription.js`
- `frontend-kjb/src/lib/api/billingKey.js`
- `frontend-kjb/src/lib/api/client.js`
- `frontend-kjb/src/lib/portone/client.js`
- `frontend-kjb/src/app/payment/checkout/page.js`
- `frontend-kjb/src/app/payment/[orderId]/page.js`
- `frontend-kjb/src/app/profile/payments/page.js`
- `frontend-kjb/src/app/subscription/page.js`
- `frontend-kjb/src/app/subscription/billing/page.js`
- `frontend-kjb/src/components/payment/CancelPaymentButton.js`
- `frontend-kjb/src/components/payment/PaymentHistoryItem.js`
