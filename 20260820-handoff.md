# 2026-08-20 파트 간 전달 사항 정리

코드 확인해서 실제로 반영됐는지 검증한 내용만 적었습니다.

## A(나) → B(ClassEnrollment) — 아직 미반영, 전달 필요

**`OneDayClassController.cancelClass()`가 여전히 `cancelPaymentByOrderId`를 쓰고 있음**
`refundForHostCancelledClass`는 `PaymentService`에 이미 추가돼 있는데(커밋에 포함됨),
컨트롤러의 GENERAL 분기가 아직 옛날 메서드(`cancelPaymentByOrderId`)를 호출 중입니다.
즉 **호스트가 클래스를 취소해도, 결제한 지 24시간이 지났거나 클래스가 이미 지났으면
여전히 환불이 안 되는 그 버그가 지금 코드에 그대로 남아있습니다.**

```java
// 현재 (line 83)
paymentService.cancelPaymentByOrderId(enrollment.getUser().getId(), enrollment.getOrderId(), null);

// 필요한 수정
paymentService.refundForHostCancelledClass(enrollment.getOrderId(), "호스트에 의한 클래스 취소");
```

**덤으로 남아있는 것**: 같은 for문에 try/catch가 없어서, 신청자 중 한 명 환불에서 PortOne
오류가 나면 나머지는 처리 안 되고 예외가 그대로 던져집니다(클래스 자체는 이미 취소 커밋된
상태라 반쯤만 처리된 애매한 상태가 됨). 개별 try/catch로 감싸서 한 명 실패해도 나머지는
계속 처리하는 걸 권장합니다.

## A(나) → C(정산/Settlement) — 확인 요청

`SettlementService.createPendingSettlement`는 여전히 `PaymentService.processPaymentSuccess`
(GENERAL 결제)에서만 호출됩니다. 구독 수강권으로 신청한 클래스(`SubscriptionService.enrollClassWithPass`)는
정산 데이터가 전혀 안 만들어집니다. 호스트 정산 조회/관리자 승인 기능은 이미 잘 만들어주셨는데
(`HostController`), 이 케이스는 아직 정책 미정 상태로 보입니다 — 스코프에 포함할지, 아니면
의도적으로 뺀 건지 확인 부탁드립니다.

## A(나) → D(프론트) — 이미 전달함, 재확인만

`20260820-todo-front.md`에 이미 남겨둔 3가지(환불 사유 라벨 매핑, 카드 변경 실패 메시지,
수강권 사용내역 QA) — 반영 여부 아직 확인 안 됨.

## 다른 파트 → A(나) — 현재 없음

B/C파트 커밋 로그(`f-110 카드 변경...`, `PaymentService - cancelPaymentByOrderId 추가...` 등)
확인해본 결과, 제 쪽에 추가로 요청된 사항은 없어 보입니다. 있으면 알려주세요.
